import mysql from 'mysql';

export function initDB() {
  const db = mysql.createConnection({
    host: 'localhost',
    user: 'root',
    password: '',
    database: 'taimtendens'
  });
  db.connect((err) => {
    if (err) {
      console.error(err);
      return;
    }
  });
  return { db };
}